## The browser history

### history oject's properties and methods

```js
History {}
	length,
	scrollRestoration,
	state,
	back(),
	forward(),
	go(),
	pushState(),
	replaceState(),
	scrollRestoration()
```
